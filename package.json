{
  "name": "nft-analytics-subgraphs",
  "version": "0.2.0",
  "description": "NFT Analytics Subgraphs",
  "license": "MIT",
  "scripts": {
    "build": "graph build",
    "auth": "graph auth --studio",
    "codegen": "graph codegen --output-dir types/",
    "deploy:loot": "yarn --cwd subgraphs/loot deploy",
    "deploy:hashmasks": "yarn --cwd subgraphs/hashmasks deploy",
    "deploy:pudgypenguins": "yarn --cwd subgraphs/pudgy-penguins deploy",
    "deploy:rarible": "yarn --cwd subgraphs/rarible deploy",
    "deploy:bayc": "yarn --cwd subgraphs/boredapeyachtclub deploy",
    "deploy:meebits": "yarn --cwd subgraphs/meebits deploy",
    "deploy:axie-infinity": "yarn --cwd subgraphs/axie-infinity deploy",
    "deploy:cryptokitties": "yarn --cwd subgraphs/cryptokitties deploy",
    "deploy:cryptopunks": "yarn --cwd subgraphs/cryptopunks deploy",
    "deploy:cryptopunks-v2": "yarn --cwd subgraphs/cryptopunks-v2 deploy",
    "deploy:known-origin": "yarn --cwd subgraphs/known-origin deploy",
    "deploy:superrare": "yarn --cwd subgraphs/superrare deploy",
    "deploy:superrare-v2": "yarn --cwd subgraphs/superrare-v2 deploy"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.1"
  }
}
