{
  "name": "nft-analytics-subgraph-cryptopunks",
  "version": "0.2.0",
  "description": "NFT Analytics Subgraph for CryptoPunks",
  "license": "MIT",
  "scripts": {
    "auth": "graph auth --studio",
    "codegen": "graph codegen --output-dir types/",
    "deploy": "graph deploy --studio nft-analytics-cryptopunks"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "latest",
    "@graphprotocol/graph-ts": "latest"
  }
}
